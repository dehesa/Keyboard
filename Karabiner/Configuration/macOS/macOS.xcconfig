// Architecture
SDKROOT = macosx

// Deployment
MACOSX_DEPLOYMENT_TARGET = 10.14.4

// Packaging
INFOPLIST_FILE = $(SRCROOT)/Configuration/macOS/macOS.plist
PRODUCT_NAME = $(KARABINER_NAME)
PRODUCT_BUNDLE_IDENTIFIER = $(KARABINER_ID).sdk
